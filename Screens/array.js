    import React, { Component } from 'react';
import {
    StyleSheet,
    Text,
    View,
    TouchableHighlight,
    Image,
} from 'react-native';
import Home from './Home';

class Array extends Component({
    componentDidMount: function() {
        resultsArrzay = [];

        if (this.sliderOptions[currentItem].value === 1){
            return 1;
            console.log("points" + 1);
        }
    }

    })
